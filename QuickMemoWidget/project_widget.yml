# Widget target definition for QuickMemo
# Merge this into the main project.yml when integrating Task-2 and Task-3

QuickMemoWidget:
  type: appExtension
  platform: iOS
  sources:
    - QuickMemoWidget
  settings:
    base:
      PRODUCT_BUNDLE_IDENTIFIER: com.iwakisyogo.QuickMemo.Widget
      INFOPLIST_KEY_CFBundleDisplayName: QuickMemo Widget
      INFOPLIST_KEY_NSHumanReadableCopyright: ""
      GENERATE_INFOPLIST_FILE: YES
      CURRENT_PROJECT_VERSION: 1
      MARKETING_VERSION: 1.0.0
      SKIP_INSTALL: YES
      ASSETCATALOG_COMPILER_WIDGET_BACKGROUND_COLOR_NAME: WidgetBackground
      LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../../Frameworks"
  dependencies:
    - target: QuickMemo
  entitlements:
    path: QuickMemoWidget/QuickMemoWidget.entitlements
